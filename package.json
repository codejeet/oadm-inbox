{
  "name": "oadm-inbox",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  }
}
